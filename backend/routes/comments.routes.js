const express = require('express');

const router = express.Router();

router.post('/addNewComment', );
router.post('/deleteComment', );
router.post('/getAllComment', );
router.post('/getCommentById', );


module.exports = router;